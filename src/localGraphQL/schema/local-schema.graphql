type User {
  id: String!
  name: String!
  email: String!
}

input UserInput {
  id: String!
  name: String!
  email: String!
}

type Query {
  user: UserInput!
}

type Mutation {
  setLocalState(user: UserInput!): UserInput!
}
